{
  "name": "rowboat-api-connector",
  "description": "Rowboat API SDK Connector - Headless webservice for Rowboat (stateless mode)",
  "dockerfile": "Dockerfile",
  "port": 8000,
  "healthCheckPath": "/health",
  "environmentVariables": [
    {
      "name": "DEBUG",
      "value": "false",
      "required": false
    },
    {
      "name": "ENABLE_OWUI",
      "value": "true",
      "required": false
    }
  ],
  "note": "This service operates in stateless mode. Rowboat credentials (HOST, API_KEY, PROJECT_ID) are provided per-request, not at deployment."
}
