# Server Configuration
PORT=8000
HOST=0.0.0.0
DEBUG=false

# OpenWebUI Integration
ENABLE_OWUI=true

# Note: Rowboat credentials (HOST, API_KEY, PROJECT_ID) are now
# provided per-request, not configured here at startup.
# This allows the service to be used with multiple Rowboat projects dynamically.
